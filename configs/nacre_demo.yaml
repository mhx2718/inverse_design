extends: nacre_paper.yaml
experiment:
  output_root: outputs/demo/nacre
  method_config_directory: configs/nacre/demo
likelihood:
  max_points: 10000
mcmc:
  burn_in_accepts: 2
  production_accepts: 4
  max_steps: 10000
  progress: false
output:
  n_output: 4
